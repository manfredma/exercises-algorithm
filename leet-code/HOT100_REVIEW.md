# LeetCode Hot 100 复盘

> 记录时间：2026-07-29。题单来源：力扣「LeetCode 热题 100」学习计划。
>
> 本文基于当前 Java 仓库中的实现进行代码复盘；不代表其他语言实现或 LeetCode 历史提交状态。

## 覆盖结论

- Hot 100 的题目此前均已完成；第 240 题此前使用 Go 完成。
- Java 题目目录已补齐第 240 题，作为 Java 版本练习入口。
- 第 146、155、208 题已有题目目录，但当前没有 Java `Solution` 实现。

## 优先补强

| 题号 | 题目 | 当前结论 | 建议掌握的方案 |
| --- | --- | --- | --- |
| 76 | 最小覆盖子串 | 当前实现存在重复的 `indexOf` 和状态扫描，最坏可能退化为 `O(|s| * |t|)`。 | 频次数组或哈希表 + 双指针滑动窗口，`O(|s| + |t|)`。 |
| 4 | 寻找两个正序数组的中位数 | 当前实现是归并式扫描，复杂度为 `O(m + n)`。 | 在较短数组上二分分割点，`O(log(min(m, n)))`。 |
| 437 | 路径总和 III | 递归中复制前缀和列表，最坏为 `O(n^2)`。 | 前缀和计数 `HashMap` + DFS，`O(n)`。 |
| 114 | 二叉树展开为链表 | 每层都扫描右链尾部，最坏为 `O(n^2)`。 | 反向前序遍历，或递归返回子树尾节点，`O(n)`。 |
| 207 | 课程表 | 默认方案反复扫描和删除依赖；另一个方案对单个自环存在过早返回风险。 | 入度数组 + 队列的 Kahn 拓扑排序；同时练习三色 DFS。 |
| 322 | 零钱兑换 | `Solution2` 在 `amount < 最小硬币` 时可能访问越界。 | 标准一维 DP：`dp[0] = 0`，逐金额转移。 |

## 已有优化版，复习时优先使用

| 题号 | 题目 | 应优先复习的方案 |
| --- | --- | --- |
| 438 | 找到字符串中所有字母异位词 | `Solution2`：滑动窗口计数。 |
| 560 | 和为 K 的子数组 | `Solution2`：前缀和 + 哈希表。 |
| 84 | 柱状图中最大的矩形 | `Solution2`：单调递增栈。 |
| 763 | 划分字母区间 | `Solution2`：记录最后位置的贪心扫描。 |
| 300 | 最长递增子序列 | `Solution3`：耐心排序 + 二分，`O(n log n)`。 |
| 394 | 字符串解码 | `Solution2`：数字栈 + 字符串栈。 |
| 347 | 前 K 个高频元素 | `Solution2`：大小为 `k` 的最小堆。 |

## 实现质量提醒

- 第 234 题当前会反转后半链表但不恢复，并可能留下环状连接；结果通常正确，但副作用过大。
- 第 15 题复杂度为 `O(n^2)`，但去重状态较重；建议复习排序 + 固定首项 + 双指针写法。
- 第 240 题此前为 Go 实现，当前 Java 目录仅提供练习骨架和示例测试。

## 后续练习顺序

```text
4 → 76 → 437 → 114 → 207 → 322
```

随后补充或实现 Java 版本：

```text
146 LRU 缓存 → 155 最小栈 → 208 Trie → 240 搜索二维矩阵 II
```
