# 4 的幂

给定一个整数，编写函数判断它是否是 `4` 的幂次方。是则返回 `true`，否则返回 `false`。

整数 `n` 是 `4` 的幂次方需满足：存在整数 `x`，使得 `n == 4^x`。

## 示例 1：

```text
输入：n = 16
输出：true
```

## 示例 2：

```text
输入：n = 5
输出：false
```

## 示例 3：

```text
输入：n = 1
输出：true
```

## 提示：

- `-2^31 <= n <= 2^31 - 1`

> 进阶：能否不使用循环或递归完成本题？
