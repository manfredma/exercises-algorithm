# 丢失的数字

> 题目链接：[LeetCode 中文站](https://leetcode.cn/problems/missing-number/)


给定一个包含 `[0, n]` 中 `n` 个数的数组 `nums`，找出 `[0, n]` 这个范围内没有出现在数组中的数字。

## 示例 1：

```text
输入：nums = [3,0,1]
输出：2
解释：n = 3，所有数字都在范围 [0,3] 内。2 是丢失的数字。
```

## 示例 2：

```text
输入：nums = [0,1]
输出：2
解释：n = 2，所有数字都在范围 [0,2] 内。2 是丢失的数字。
```

## 示例 3：

```text
输入：nums = [9,6,4,2,3,5,7,0,1]
输出：8
解释：n = 9，所有数字都在范围 [0,9] 内。8 是丢失的数字。
```

## 提示：

- `n == nums.length`
- `1 <= n <= 10^4`
- `0 <= nums[i] <= n`
- `nums` 中的所有数字都独一无二。

> 进阶：能否使用线性时间和常量额外空间解决此问题？
